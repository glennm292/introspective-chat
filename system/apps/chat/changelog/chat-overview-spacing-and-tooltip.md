Two small fixes to the Agent Overview.

The tokens table now leaves a gap beneath it, so its bottom rule no longer sits directly on the Close button.

The hover tooltip's width cap rises from 480px to 560px, which pulls the timing explanation's orphaned final line ("in the same gap is counted with it.") up from two words to eight. The cap only binds on a tooltip long enough to reach it -- a short label wraps at its own natural width well below it -- so no other tooltip changes.
