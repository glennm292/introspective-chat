The Agent Overview loses its explanatory paragraphs.

"Tokens" moves into the data row, in the same first column the activity table labels its rows in, so the two tables read as one grid rather than two.

The explanation of what the times measure moves into a circled "?" beside the Total header, where it qualifies the column it belongs to instead of sitting under the panel for every reader to scroll past.

The paragraph about tool calls not using tokens is gone. One fact went with it and is worth recording here: the Input figure counts tokens written into the cache as well as fresh ones, because the model read those for the first time too.
